# CLAUDE.md

本文档为 Claude Code 提供 {{ project_name }} 项目的开发指南。

## 项目概况

{{ project_description }}

FastAPI 基础模板项目，采用 **feature-based 架构**，按业务领域划分代码。包管理器使用 `uv`，Python 版本 3.12。

## 常用命令

```bash
# 安装依赖后锁定版本（宿主机执行）
uv lock

# 启动全部服务
docker-compose up -d

# 查看 API 容器日志
docker-compose logs -f api

# 进入容器执行 alembic 迁移
docker-compose exec api alembic upgrade head
docker-compose exec api alembic revision --autogenerate -m "描述"

# 运行测试
docker-compose exec api pytest tests/ -v
```

> **注意**：除 `uv lock` 外，所有操作都在容器内执行。

## 架构概览

```
app/
├── main.py                     # 入口：lifespan、异常处理、CORS、自动路由注册
{% if 'celery' in features %}├── celery_app.py               # Celery 配置（Manifest 自动发现任务）
{% endif %}├── core/
│   ├── config.py               # Pydantic BaseSettings，按模块 Mixin 组合
│   ├── postgresql.py           # 异步/同步 DB 管理器
{% if 'redis' in features %}│   ├── redis.py                # 异步/同步 Redis 管理器
{% endif %}│   └── init_db.py             # 启动时自动 alembic + admin 创建
├── shared/
│   ├── models.py               # BaseMixin、BaseResponse[T]
│   ├── utils.py                # ULID/UTC 时间工具
│   ├── logs.py                 # Loguru 配置
│   └── health.py               # 健康检查（Manifest 自动注册）
{% if 'radar' in features %}│   ├── radar.py                # HTTP 监控面板配置
{% endif %}{% if 'auth' in features %}│   ├── admin.py                # SQLAdmin 挂载
│   ├── admin_auth.py           # JWT admin 认证
│   └── admin_views.py          # 模型管理视图
{% endif %}└── features/                   # 按业务领域划分
{% if 'auth' in features %}    └── auth/
        ├── manifest.py           # Feature Manifest（路由、任务、模型声明）
        ├── router.py
        ├── models.py
        ├── dependencies.py
        ├── tasks.py
        └── service/
{% endif %}    └── [其他feature]/
```

## 核心约定

### Feature Manifest 模式
每个 feature 目录下应包含 `manifest.py`，统一声明：
- `router_module` / `router_prefix` / `router_tags` - 路由信息
- `tasks_package` / `beat_schedule` - Celery 任务
- `models_module` - 数据库模型
- `admin_views_module` - Admin 视图
- `health_checks` - 健康检查

### 导入规范
- **同一 feature 内**：相对导入
- **跨 feature / 共享模块**：绝对导入

### 分层职责
- **Router 层**：无 `try-except`，直接调用 service
- **Service 层**：业务验证抛 `HTTPException`，数据库操作 `try-except SQLAlchemyError`
- **全局处理器**（`main.py`）：捕获所有异常并格式化为 `BaseResponse`

### 响应格式
所有端点统一返回 `BaseResponse[T]`，包含 `data` 和 `message` 字段。

### Redis 缓存
- 键命名：`{{ project_slug }}:模块:功能:参数`
- 过期时间通过 `.env` 统一管理
- 写入使用 `model_dump_json()`，读取使用 `model_validate_json()`

### 模型构建（BaseMixin 模式）
- **BaseMixin**（无 `table=True`）：通用字段
- **业务 Mixin**（无 `table=True`）：业务字段
- **表模型**（`table=True`）：继承 BaseMixin + 业务 Mixin

## 服务端口
- FastAPI API: `{{ api_host_port }}`（容器内 `8000`）
- PostgreSQL: `{{ db_port }}`（容器内 `5432`）
{% if 'redis' in features %}- Redis: `{{ redis_host_port }}`（容器内 `6379`）
{% endif %}
{% if 'auth' in features %}## Admin 面板
- 路径：`/admin`
- 认证：JWT-based，检查 `is_admin` 标志
{% endif %}