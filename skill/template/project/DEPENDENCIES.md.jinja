# 模块依赖校验清单

> 生成项目后，AI 应检查以下依赖关系是否满足。如不满足，需提醒用户并自动修复。

## 依赖规则

| 模块 | 依赖模块 | 说明 |
|------|---------|------|
| celery | redis | Celery 需要 Redis 作为消息代理 |
| admin | 无 | SQLAdmin 可独立使用 |
| auth | 无 | 认证模块无外部依赖 |
| redis | 无 | 缓存层无外部依赖 |
| storage | 无 | 对象存储无外部依赖 |
| email | 无 | 邮件服务无外部依赖 |
| radar | 无 | 监控面板无外部依赖 |

## 校验逻辑

检查 `features` 列表：
- 如果包含 `celery` 但不包含 `redis` → **错误**：自动添加 redis 到 features 并告知用户
- 其他组合均合法

## 生成后自动执行

1. 读取 `.copier-answers.yml` 获取已选择的 features
2. 检查上述规则
3. 如发现问题，自动修正并重新生成受影响文件
